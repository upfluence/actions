name: 'bump version'
description: ''
inputs:
  strategy:
    description: 'bumping strategy to be used, available strategies are: bump_major, bump_minor, bump_patch, bump_rc'
    required: false
    default: ''
  strategies-by-branch:
    description: 'override the bumping strategy on a per branch basis, you can pass it as master=bump_major,staging=bump_minor'
    required: false
    default: ''
  github-token:
    required: false
    description: 'github token to be used'
    default: ${{ github.token }}
  cli-version:
    required: false
    description: 'version of the cli to use'
    default: 'latest'
outputs:
  version:
    description: 'the target version'
    value: ${{ steps.bump-version.outputs.version }}
runs:
  using: 'composite'
  steps:
    - run: |
        mkdir -p ~/go/bin
        if [ "${{ inputs.cli-version }}" = "latest" ]; then
          curl -L https://github.com/upfluence/actions/releases/latest/download/bump-version > ~/go/bin/bump-version
        else
          curl -L https://github.com/upfluence/actions/releases/download/${{ inputs.cli-version }}/bump-version > ~/go/bin/bump-version
        fi
      shell: bash
    - run: chmod +x ~/go/bin/bump-version
      shell: bash
    - id: bump-version
      run: ~/go/bin/bump-version --strategy ${{ inputs.strategy }} --strategies-by-branch ${{ inputs.strategies-by-branch }}
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github-token }}
